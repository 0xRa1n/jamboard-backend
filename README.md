# Jamboard Backend

This is the backend service for Jamboard, built with Node.js, Express, and PostgreSQL.

## Steps for pushing code

```
npm audit
npm test

to avoid skipping tests, use node --env-file=.env.local --test

git add .
git commit -m "<message>"
git push origin main
```
